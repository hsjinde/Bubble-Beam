# 深色模式 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 讓 `/decks` 與 `/pokopia` 兩個閱讀型子站支援深色模式，預設跟隨系統偏好，並提供系統／淺色／深色三態手動覆寫且記住選擇。

**Architecture:** 三層——(1) `styles.css` 用 class-based `@custom-variant dark` ＋ `.dark {}` 區塊翻 token 值，14 個既有 token 翻值即級聯到所有消費端；(2) `src/lib/theme.ts` 管三態解析、localStorage 與系統偏好監聽；(3) `__root.tsx` 的 `<head>` inline script 在首次繪製前搶先寫 class，避免 SSR 白閃。首頁刻意不參與，靠 `data-theme="light"` 子樹 token 重設隔離。

**Tech Stack:** TanStack Start (SSR)、React 19、Tailwind CSS v4.2.1、TypeScript。無新增相依。

設計依據：[docs/superpowers/specs/2026-07-24-dark-mode-design.md](../specs/2026-07-24-dark-mode-design.md)

## Global Constraints

每個任務的要求都隱含包含這一節。值全部逐字抄自 spec 與 `CLAUDE.md` / `core_rules.md`。

- **淺色模式算繪值必須逐位元組不變。** 這次改動前後，淺色模式下每個元素的 `getComputedStyle` 結果要完全相同。
- **`/decks`（含展開區）對比 0 處失敗是硬底線**，深色模式也要 0。不得退化。
- **對比一律量算繪後顏色**，不能只看 CSS 原始值——`oklch()`、透明度疊加、Tailwind 色階都要算繪後才準。
- **跑 dev server 一律用 `preview_start`（config name `piplup-dev`，見 `.claude/launch.json`），不要用 Bash 起。**
- **本專案沒有測試框架**（package.json 無 test script，無 vitest/jest）。驗證方式是瀏覽器實測。**不要為了「跑測試」去裝測試框架。**
- **前端不要 `import cards.json`**（3520 張、約 580 KB）。查卡一律走 `getCard()`。
- **生成檔不要手改**：`cards.json`、`cards.used.json`、`meta.json`、`routeTree.gen.ts`、`sitemap.xml`、`robots.txt`。
- **不要 force push / rebase / amend / squash 已推送的歷史**（會弄壞 Lovable 端的歷史）。
- **不要加任何新相依**。`bunfig.toml` 設了 `minimumReleaseAge = 86400`，要加例外前先問使用者。
- **觸控目標 ≥44×44px**，手機 375px 不得有水平溢位。
- 攻略頁配色走 `--guide-*` / `--pokopia-*` token，**不是** `styles.css` 的 oklch design system（那組只有 404／error 頁在用）。
- Node >= 22.6.0（`prebuild` 的 `--experimental-strip-types` 需要）。
- 介面文字用繁體中文；牌組名與卡名保留 Limitless 英文原文；程式碼識別字一律英文。

---

## 共用驗證工具

以下三段在多個任務重複使用。**在瀏覽器 console 執行**（先用 `preview_start` 起 `piplup-dev`）。

### 工具 A：逐節點文字對比量測

處理了透明度疊加（背景往上合成到不透明祖先）與祖先 `opacity` 乘積——`core_rules.md` 明訂不能只看 CSS 原始值。

```js
window.__contrast = () => {
  const num = (s) => (s.match(/[\d.]+/g) || []).map(Number);
  const lum = ([r, g, b]) => {
    const f = (c) => {
      c /= 255;
      return c <= 0.03928 ? c / 12.92 : Math.pow((c + 0.055) / 1.055, 2.4);
    };
    return 0.2126 * f(r) + 0.7152 * f(g) + 0.0722 * f(b);
  };
  const ratio = (a, b) => {
    const [hi, lo] = [lum(a), lum(b)].sort((x, y) => y - x);
    return (hi + 0.05) / (lo + 0.05);
  };
  // 背景：往上收集半透明層，直到遇到不透明的，再由下往上合成
  const bgOf = (el) => {
    const layers = [];
    for (let n = el; n; n = n.parentElement) {
      const c = num(getComputedStyle(n).backgroundColor);
      if (c.length < 3) continue;
      const a = c.length > 3 ? c[3] : 1;
      if (a > 0) layers.push([c[0], c[1], c[2], a]);
      if (a === 1) break;
    }
    let base = [255, 255, 255];
    for (let i = layers.length - 1; i >= 0; i--) {
      const [r, g, b, a] = layers[i];
      base = [r * a + base[0] * (1 - a), g * a + base[1] * (1 - a), b * a + base[2] * (1 - a)];
    }
    return base;
  };
  const fails = [];
  let checked = 0;
  for (const el of document.querySelectorAll("*")) {
    const txt = [...el.childNodes]
      .filter((n) => n.nodeType === 3 && n.textContent.trim())
      .map((n) => n.textContent.trim())
      .join("");
    if (!txt) continue;
    const cs = getComputedStyle(el);
    if (cs.visibility === "hidden" || cs.display === "none") continue;
    if (el.closest("[hidden]") || el.classList.contains("sr-only")) continue;
    // 祖先 opacity 乘積 ＋ 文字色自身 alpha
    let alpha = 1;
    for (let n = el; n; n = n.parentElement) alpha *= Number(getComputedStyle(n).opacity);
    const fg = num(cs.color);
    if (fg.length > 3) alpha *= fg[3];
    if (alpha === 0) continue;
    const bg = bgOf(el);
    const eff = [0, 1, 2].map((i) => fg[i] * alpha + bg[i] * (1 - alpha));
    const size = parseFloat(cs.fontSize);
    const weight = Number(cs.fontWeight) || 400;
    const need = size >= 24 || (size >= 18.66 && weight >= 700) ? 3 : 4.5;
    const r = ratio(eff, bg);
    checked++;
    if (r < need)
      fails.push({
        text: txt.slice(0, 28),
        ratio: +r.toFixed(2),
        need,
        color: cs.color,
        cls: String(el.className).slice(0, 60),
      });
  }
  return { checked, failCount: fails.length, fails };
};
__contrast();
```

預期輸出形如 `{checked: 308, failCount: 0, fails: []}`。**`failCount` 必須是 0。**

### 工具 B：算繪值快照（驗證淺色模式零變化）

```js
window.__snapshot = () => {
  const out = [];
  document.querySelectorAll("*").forEach((el, i) => {
    const cs = getComputedStyle(el);
    out.push(
      [i, el.tagName, cs.color, cs.backgroundColor, cs.borderTopColor, cs.borderBottomColor].join(
        "|",
      ),
    );
  });
  return out.join("\n");
};
copy(__snapshot()); // 複製到剪貼簿，貼到檔案存起來比對
```

用法：改動**前**在每個頁面跑一次存檔，改動**後**再跑一次，兩份必須逐字元相同。

### 工具 C：手動切換 `.dark`（切換 UI 完成前用這個驗深色）

```js
document.documentElement.classList.toggle("dark");
```

---

## Task 1: guide 側 token 收斂（淺色零變化）

把 `/decks` 側會在深色模式壞掉的硬編色收斂成語意 token。**這一步不引入任何深色值**，交付物是「重構完成、視覺零變化」。

**Files:**

- Modify: `src/styles.css`（`@theme inline` 與 `:root` 各加 4 個 token）
- Modify: `src/components/guide/MetaRanking.tsx`、`ScheduleBoard.tsx`、`CuratedDecks.tsx`、`DeckCard.tsx`、`CopyDecklist.tsx`、`Decklist.tsx`、`CardUsagePanel.tsx`、`NextSetBanner.tsx`、`RankChangeBadge.tsx`、`GuideLayout.tsx`
- Modify: `src/routes/decks/$deckId.tsx`、`src/routes/decks/index.tsx`、`src/routes/decks/schedule.tsx`

**Interfaces:**

- Produces: 四個 Tailwind utility 前綴，後續任務會在 `.dark` 區塊翻它們的值——`bg-guide-surface`、`text-guide-ink-body`、`text-guide-ink-muted`、`text-guide-on-ink`。

- [ ] **Step 1: 先存淺色基準快照**

用 `preview_start` 起 `piplup-dev`，在 console 貼上「工具 B」，對這四頁各跑一次並存檔：
`/decks`、`/decks/schedule`、任一攻略頁（例如 `/decks/mega-blaziken-ex`）、`/pokopia`。

存到 `C:\Users\DE\AppData\Local\Temp\claude\D--Bubble-Beam\<session>\scratchpad\before-<page>.txt`。

- [ ] **Step 2: 在 `styles.css` 註冊四個新 token**

在 `@theme inline` 區塊，`--color-guide-bg-highlight` 那行之後加：

```css
--color-guide-surface: var(--guide-surface);
--color-guide-ink-body: var(--guide-ink-body);
--color-guide-ink-muted: var(--guide-ink-muted);
--color-guide-on-ink: var(--guide-on-ink);
```

在 `:root` 區塊，`--guide-bg-highlight: #f4fbff;` 那行之後加：

```css
/*
   * 深色模式收斂進來的四個 token。淺色值逐位元組沿用原本的硬編值
   * （bg-white / text-slate-700 / text-slate-600 / text-white），
   * 淺色模式算繪結果不變。深色值見 .dark 區塊。
   *
   * on-ink 是「壓在 bg-guide-ink 上的前景色」。深色模式下 --guide-ink
   * 會翻成淺藍，白字會爆掉，所以前景必須跟著翻——沿用 Material 的
   * on-<color> 命名慣例。
   */
--guide-surface: #ffffff;
--guide-ink-body: #334155;
--guide-ink-muted: #475569;
--guide-on-ink: #ffffff;
```

- [ ] **Step 3: 替換 `bg-white` → `bg-guide-surface`**

先列出所有命中處（**排除 `src/components/GuideEntry.tsx` 與 `PokopiaEntry.tsx`，那是首頁元件，見 Task 5**）：

```bash
grep -rn "bg-white" src/components/guide src/routes/decks
```

預期 15 處。逐一替換，**保留透明度修飾**：`bg-white/70` → `bg-guide-surface/70`、`bg-white/90` → `bg-guide-surface/90`。

Tailwind v4 對十六進位 token 支援 opacity modifier（會轉成 `color-mix`），算繪結果與 `bg-white/70` 相同。

- [ ] **Step 4: 替換 slate 文字色**

```bash
grep -rn "text-slate-700" src/components/guide src/routes/decks
grep -rn "text-slate-600" src/components/guide src/routes/decks
```

- `text-slate-700` → `text-guide-ink-body`（預期 10 處）
- `text-slate-600` → `text-guide-ink-muted`（預期 28 處）

**兩者不能合併成同一個 token**——`#334155` 與 `#475569` 是兩個不同明度階，合併會改到淺色算繪值。

- [ ] **Step 5: 替換 `bg-guide-ink` 上的 `text-white`**

```bash
grep -rnE "bg-guide-ink" src/components/guide src/routes/decks
```

預期 10 處，其中 **8 處帶 `text-white`**，全部改成 `text-guide-on-ink`：
`CardUsagePanel.tsx:28`、`CuratedDecks.tsx:29`、`Decklist.tsx:13`、`MetaRanking.tsx:65`、`MetaRanking.tsx:180`、`NextSetBanner.tsx:25`、`RankChangeBadge.tsx:37`、`ScheduleBoard.tsx:48`。

**另外 2 處不要動**——`CardUsagePanel.tsx:41` 與 `PokopiaPage.tsx:90` 是沒有前景文字的進度條填色，不需要 `on-*` token（它們的對比在 Task 10 另外驗）。

- [ ] **Step 6: 確認沒有殘留**

```bash
grep -rn "bg-white\|text-slate-600\|text-slate-700" src/components/guide src/routes/decks
```

預期輸出：**無命中**（註解裡的字串除外，`RankChangeBadge.tsx` 第 9 行的註解提到 `text-slate-300` 是歷史記錄，保留不動）。

- [ ] **Step 7: 驗證淺色模式零變化**

重跑「工具 B」對 Step 1 的四個頁面各取一次快照，與 `before-*.txt` 逐字元比對：

```bash
diff before-decks.txt after-decks.txt && echo "零變化"
```

預期：**四份都 `diff` 無輸出**。有差異就是替換錯了值，回到 Step 3–5 修正。

- [ ] **Step 8: 型別與 lint**

```bash
npx tsc --noEmit && npm run lint
```

預期：兩者都 0 錯誤。

- [ ] **Step 9: Commit**

```bash
git add src/styles.css src/components/guide src/routes/decks
git commit -m "refactor(theme): guide 側硬編色收斂成語意 token

bg-white / text-slate-600 / text-slate-700 / bg-guide-ink 上的 text-white
收斂成 --guide-surface / --guide-ink-muted / --guide-ink-body / --guide-on-ink。
淺色值逐位元組沿用原值，四個頁面的 getComputedStyle 快照 diff 為空。

深色模式的前置作業：這些 token 之後會在 .dark 區塊翻值。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: pokopia 側 token 收斂（淺色零變化）

與 Task 1 同性質，但 pokopia 側只需要兩個新 token——它已經有 `--pokopia-ink-soft`，且沒有任何 `text-slate-*`。

**Files:**

- Modify: `src/styles.css`
- Modify: `src/components/pokopia/BuildingFilters.tsx`、`HabitatLookup.tsx`、`PokopiaPage.tsx`、`CollectionCard.tsx`、`BuildingDetail.tsx`、`BuildingList.tsx`

**Interfaces:**

- Consumes: Task 1 建立的 token 命名慣例（`surface` / `on-*`）。
- Produces: `bg-pokopia-surface`、`text-pokopia-on-accent` 兩個 utility。

- [ ] **Step 1: 在 `styles.css` 註冊兩個新 token**

`@theme inline` 區塊，`--color-pokopia-highlight` 那行之後：

```css
--color-pokopia-surface: var(--pokopia-surface);
--color-pokopia-on-accent: var(--pokopia-on-accent);
```

`:root` 區塊，`--pokopia-highlight: #fbe8dd;` 那行之後：

```css
/* 與 guide 側同構：surface 承接 bg-white，on-accent 承接壓在
   * bg-pokopia-accent 上的白字（深色模式下 accent 會提亮，白字會爆掉）。 */
--pokopia-surface: #ffffff;
--pokopia-on-accent: #ffffff;
```

- [ ] **Step 2: 替換 `bg-white`**

```bash
grep -rn "bg-white" src/components/pokopia src/routes/pokopia
```

預期 8 處，全部 → `bg-pokopia-surface`（保留透明度修飾）。

**注意 `BuildingFilters.tsx:122` 是開關的軌道底**（`bg-white` ＋ `peer-checked:bg-pokopia-accent`），一樣要換。

- [ ] **Step 3: 替換 `bg-pokopia-accent` 上的 `text-white`**

```bash
grep -rnE "bg-pokopia-accent" src/components/pokopia src/routes/pokopia
```

預期 7 處帶白字，改成 `text-pokopia-on-accent`：
`BuildingDetail.tsx:52`、`BuildingFilters.tsx:156`、`BuildingFilters.tsx:173`、`HabitatLookup.tsx:139`、`HabitatLookup.tsx:146`、`PokopiaPage.tsx:139`、`BuildingList.tsx:75`（`peer-checked:text-white` → `peer-checked:text-pokopia-on-accent`）。

**`PokopiaPage.tsx:90` 不要動**——進度條填色，無前景文字。

- [ ] **Step 4: 確認 `text-white` 只剩下該留的那一處**

```bash
grep -rn "text-white" src/components/pokopia
```

預期：只剩 `VideoInspiration.tsx:99`（`bg-black/55` 實心黑底，不翻值，維持白字）。

- [ ] **Step 5: 驗證淺色模式零變化**

用「工具 B」對 `/pokopia`、`/pokopia/videos`、`/pokopia/habitats` 各取快照，與改動前比對。改動前的基準若沒存，先 `git stash` 取回原狀取一次再 `git stash pop`。

預期：**三份 `diff` 都無輸出**。

- [ ] **Step 6: 型別與 lint**

```bash
npx tsc --noEmit && npm run lint
```

預期：0 錯誤。

- [ ] **Step 7: Commit**

```bash
git add src/styles.css src/components/pokopia src/routes/pokopia
git commit -m "refactor(theme): pokopia 側硬編色收斂成語意 token

bg-white → --pokopia-surface；bg-pokopia-accent 上的白字 → --pokopia-on-accent。
淺色值逐位元組沿用原值，三個頁面的 getComputedStyle 快照 diff 為空。

VideoInspiration 的 bg-black/55 白字刻意保留——實心黑底不翻值。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: `@custom-variant` ＋ guide 側深色值

第一次讓深色模式真的看得見。切換 UI 還沒做，用「工具 C」手動加 class 驗證。

**Files:**

- Modify: `src/styles.css`

**Interfaces:**

- Consumes: Task 1 的四個新 guide token。
- Produces: `.dark` 區塊與 `@custom-variant dark`，後續任務的 `dark:` utility 靠它生效。

- [ ] **Step 1: 宣告 class-based dark variant**

在 `styles.css` 頂端，`@import "tw-animate-css";` 之後加：

```css
/*
 * Tailwind v4 的 dark: variant 預設綁 prefers-color-scheme，跟 class 無關。
 * 本站要支援「系統／淺色／深色」三態手動覆寫，必須改成 class-based。
 * 沒有這一行的話，所有 dark: utility 都只會跟隨系統，手動切換無效。
 */
@custom-variant dark (&:where(.dark, .dark *));
```

- [ ] **Step 2: 更新檔頭註解**

`styles.css` 開頭那段註解目前寫「There is no .dark variant」，已經過期。替換成：

```css
/*
 * Design system definition.
 *
 * The @theme inline block maps CSS custom properties to Tailwind utility
 * classes (e.g. --color-primary -> bg-primary, text-primary).
 *
 * :root defines the light values; the .dark block below overrides them.
 * Dark mode is class-based (@custom-variant dark at the top of this file),
 * driven by src/lib/theme.ts and the inline script in __root.tsx.
 *
 * To add a new semantic color:
 * 1. Add the variable to :root
 * 2. Add its dark counterpart to .dark
 * 3. Register it in @theme inline as --color-<name>: var(--<name>)
 *
 * 深色值一律是「同色相的去飽和／明度變體」，不是把顏色反轉。改任何值之後
 * 都要在兩個模式各量一次算繪後對比（見 docs/handoff.md）。
 */
```

- [ ] **Step 3: 加 guide 側的 `.dark` 區塊**

放在 `:root {}` 之後、`@layer base` 之前：

```css
.dark {
  /*
   * /decks 水藍系深色值（色相 ≈ 205°，與淺色同色相）。
   *
   * 兩個語意反轉，改值時容易配反：
   *   ink-deep 是「更強調」——淺色模式靠壓深，深色模式靠**提亮**。
   *   bg-highlight 是「比一般列突出」——深色模式要比 bg **更亮**，不是更暗。
   *
   * accent 維持 #5fa8d3：它在深藍底上本來就達標，沒有換值的理由。
   */
  --guide-bg: #0f1a22;
  --guide-bg-panel: #16242f;
  --guide-bg-highlight: #1a2c39;
  --guide-surface: #1e313f;
  --guide-ink: #9fd0ea;
  --guide-ink-deep: #cfe9f7;
  --guide-ink-body: #b3c6d3;
  --guide-ink-muted: #93a9ba;
  --guide-accent: #5fa8d3;
  --guide-tint: #2e4b5e;
  /* 壓在 bg-guide-ink（深色模式下是淺藍）上的前景色 */
  --guide-on-ink: #0f1a22;
}
```

- [ ] **Step 4: 手動開深色模式，量對比**

在 `/decks` 的 console：

```js
document.documentElement.classList.add("dark");
__contrast();
```

（`__contrast` 的定義見「共用驗證工具」工具 A，每次重新整理後要重貼。）

預期：`failCount: 0`。**不是 0 的話**，看 `fails` 陣列裡每筆的 `ratio` 與 `cls`，調整對應 token 的深色值——原則是背景往暗調、文字往亮調，維持色相不變。調完重跑直到 0。

- [ ] **Step 5: 同樣量攻略頁與行事曆頁**

對 `/decks/mega-blaziken-ex`（或任一攻略頁）與 `/decks/schedule` 重複 Step 4。

**要記得展開排行榜的列**（點幾列展開再量），展開區的文字是 `core_rules.md` 明列的必驗範圍。

預期：兩頁都 `failCount: 0`。

- [ ] **Step 6: 檢查邊框與焦點環**

```js
// 邊框對背景（WCAG 1.4.11 非文字 3:1）
getComputedStyle(document.querySelector("header")).borderBottomColor;
// 焦點環：Tab 到第一個連結，看 outline 是否可見
```

`--guide-tint`（`#2e4b5e`）對 `--guide-bg`（`#0f1a22`）的對比要 ≥3:1。不足就把 tint 調亮。

焦點環用的是 `--guide-ink`（深色模式下是 `#9fd0ea` 淺藍），在深底上應該清晰。截圖確認。

- [ ] **Step 7: 切回淺色確認沒壞**

```js
document.documentElement.classList.remove("dark");
__contrast();
```

預期：`failCount: 0`（淺色模式的底線不得退化）。

- [ ] **Step 8: Commit**

```bash
git add src/styles.css
git commit -m "feat(theme): 加入 class-based dark variant 與 guide 側深色值

@custom-variant dark (&:where(.dark, .dark *)) —— Tailwind v4 的 dark:
預設綁 prefers-color-scheme，三態手動覆寫必須改成 class-based。

/decks 深色值延續 205° 色相去飽和，非反轉。手動加 .dark 後實測
排行榜（含展開列）、攻略頁、行事曆頁對比全數 0 失敗。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: pokopia 側深色值 ＋ shadcn oklch 深色值

**Files:**

- Modify: `src/styles.css`（擴充 Task 3 建立的 `.dark` 區塊）

**Interfaces:**

- Consumes: Task 3 的 `.dark` 區塊、Task 2 的兩個 pokopia token。

- [ ] **Step 1: 在 `.dark` 區塊補 pokopia 深色值**

接在 Task 3 那段 guide 值之後（同一個 `.dark {}` 內）：

```css
/*
   * /pokopia 暖棕系深色值（色相 ≈ 27°）。與 /decks 的水藍區隔在深色模式維持。
   *
   * accent 是唯一必須換值的點綴色：#a35f1f 在深棕底上亮度差太小，提亮到 #d98b3f。
   * （對照：guide 的 accent 不用換。）
   */
--pokopia-bg: #1a1512;
--pokopia-bg-panel: #241d17;
--pokopia-surface: #2b221b;
--pokopia-highlight: #33251b;
--pokopia-ink: #ecdcc9;
--pokopia-ink-soft: #bfa78d;
--pokopia-accent: #d98b3f;
--pokopia-tint: #4a3b2c;
--pokopia-on-accent: #1a1512;
```

- [ ] **Step 2: 在 `.dark` 區塊補 shadcn oklch 深色值**

只有 404 與 error 頁在用這組。用 shadcn 的標準深色值，不重新調配：

```css
/*
   * shadcn/oklch design system 的深色值（只有 404 與 error 頁在用）。
   * 直接沿用 shadcn 標準值——這兩頁不是本站的設計重點，驗過對比即可。
   * 不補的話，深色模式下進 404 頁會整片爆白。
   */
--background: oklch(0.129 0.042 264.695);
--foreground: oklch(0.984 0.003 247.858);
--card: oklch(0.208 0.042 265.755);
--card-foreground: oklch(0.984 0.003 247.858);
--popover: oklch(0.208 0.042 265.755);
--popover-foreground: oklch(0.984 0.003 247.858);
--primary: oklch(0.929 0.013 255.508);
--primary-foreground: oklch(0.208 0.042 265.755);
--secondary: oklch(0.279 0.041 260.031);
--secondary-foreground: oklch(0.984 0.003 247.858);
--muted: oklch(0.279 0.041 260.031);
--muted-foreground: oklch(0.704 0.04 256.788);
--accent: oklch(0.279 0.041 260.031);
--accent-foreground: oklch(0.984 0.003 247.858);
--destructive: oklch(0.704 0.191 22.216);
--destructive-foreground: oklch(0.984 0.003 247.858);
--border: oklch(1 0 0 / 10%);
--input: oklch(1 0 0 / 15%);
--ring: oklch(0.551 0.027 264.364);
--chart-1: oklch(0.488 0.243 264.376);
--chart-2: oklch(0.696 0.17 162.48);
--chart-3: oklch(0.769 0.188 70.08);
--chart-4: oklch(0.627 0.265 303.9);
--chart-5: oklch(0.645 0.246 16.439);
--sidebar: oklch(0.208 0.042 265.755);
--sidebar-foreground: oklch(0.984 0.003 247.858);
--sidebar-primary: oklch(0.488 0.243 264.376);
--sidebar-primary-foreground: oklch(0.984 0.003 247.858);
--sidebar-accent: oklch(0.279 0.041 260.031);
--sidebar-accent-foreground: oklch(0.984 0.003 247.858);
--sidebar-border: oklch(1 0 0 / 10%);
--sidebar-ring: oklch(0.551 0.027 264.364);
```

- [ ] **Step 3: 量 pokopia 三頁的深色對比**

對 `/pokopia`、`/pokopia/videos`、`/pokopia/habitats` 各跑：

```js
document.documentElement.classList.add("dark");
__contrast();
```

預期：三頁都 `failCount: 0`。

`/pokopia` 有篩選器與建築詳情面板，量之前先展開／點開，讓那些節點進入 DOM。

- [ ] **Step 4: 量 404 頁的深色對比**

導到一個不存在的網址（例如 `/no-such-page`），加 `.dark` 後跑 `__contrast()`。

預期：`failCount: 0`。

- [ ] **Step 5: 切回淺色三頁 ＋ 404 都重量一次**

預期：全部 `failCount: 0`。

- [ ] **Step 6: Commit**

```bash
git add src/styles.css
git commit -m "feat(theme): 加入 pokopia 與 shadcn oklch 的深色值

pokopia 深色延續 27° 暖棕；accent 必須從 #a35f1f 提亮到 #d98b3f，
否則在深棕底上亮度差不足（guide 側的 accent 則不用換）。

shadcn oklch 那組沿用上游標準深色值——只有 404／error 頁在用，
不補的話深色模式下進 404 會整片爆白。

四個頁面（含 404）雙模式對比實測 0 失敗。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: 首頁隔離

`.dark` 掛在 `<html>` 上是全站生效的，而首頁大量讀 guide token。不隔離的話，深色模式下 `GuideEntry` 的便條紙會是白底配 `#cfe9f7` 淺藍字。

**Files:**

- Modify: `src/routes/index.tsx:31`
- Modify: `src/styles.css`

**Interfaces:**

- Consumes: Task 3 的 `.dark` 區塊。
- Produces: `data-theme="light"` 子樹隔離機制，以及「首頁不得使用 `dark:` utility」這條不變式。

- [ ] **Step 1: 首頁根元素加 `data-theme="light"`**

`src/routes/index.tsx` 第 31 行：

```tsx
    <main
      data-theme="light"
      className="fixed inset-0 overflow-hidden"
      aria-label="波加曼粉絲遊樂場首頁"
    >
```

- [ ] **Step 2: 在 `styles.css` 加隔離規則**

放在 `.dark {}` 區塊之後：

```css
/*
 * 首頁不參與深色模式：它是全螢幕影片背景＋手繪塗鴉的遊樂場，沒有可反轉的
 * 主題表面。但它**讀 guide token**（index.tsx 的標題與 tint 疊層、GuideEntry
 * 的文字、Doodles 的線條、VideoBackdrop 的 fallback 底），而 .dark 掛在
 * <html> 上是全站生效的——不隔離的話，GuideEntry 的便條紙會變成白底配淺藍字。
 *
 * 自訂屬性沿 DOM 樹繼承（不是視覺樹），所以 GuideEntry／PokopiaEntry 雖然是
 * fixed 定位，仍在 <main> 的子樹內，涵蓋得到。
 *
 * ⚠ 這個隔離只擋 token，擋不了 dark: utility——.dark 仍在 <html> 上。
 * 因此立一條不變式：**首頁的五個檔案（routes/index.tsx、GuideEntry、
 * PokopiaEntry、Doodles、VideoBackdrop）不得使用 dark: utility。**
 * 要改首頁配色就直接改值。破壞這條的症狀只在「深色模式的首頁」出現，很難聯想。
 */
.dark [data-theme="light"] {
  --guide-ink: #2a6f97;
  --guide-ink-deep: #1d5273;
  --guide-accent: #5fa8d3;
  --guide-tint: #bfe3f5;
  --guide-bg: #eef7fc;
  --guide-bg-panel: #f8fcff;
  --guide-bg-highlight: #f4fbff;
  --guide-surface: #ffffff;
  --guide-ink-body: #334155;
  --guide-ink-muted: #475569;
  --guide-on-ink: #ffffff;
}
```

- [ ] **Step 3: 確認首頁目前 0 處 `dark:`**

```bash
grep -rn "dark:" src/routes/index.tsx src/components/GuideEntry.tsx src/components/PokopiaEntry.tsx src/components/Doodles.tsx src/components/VideoBackdrop.tsx
```

預期：**無命中**。有命中就是違反了 Step 2 註解裡的不變式，改成直接寫值。

- [ ] **Step 4: 驗證隔離有效**

在首頁 `/` 的 console：

```js
document.documentElement.classList.add("dark");
// 便條紙的文字色應該還是深藍
getComputedStyle(document.querySelector('[aria-label*="牌組攻略"] span')).color;
```

預期：`rgb(29, 82, 115)`（`#1d5273`），**不是** `rgb(207, 233, 247)`。

再跑 `__contrast()`，預期 `failCount: 0`。

- [ ] **Step 5: 淺色模式下首頁也重量一次**

```js
document.documentElement.classList.remove("dark");
__contrast();
```

預期：`failCount: 0`，且第 4 步那個 color 值不變。

- [ ] **Step 6: Commit**

```bash
git add src/routes/index.tsx src/styles.css
git commit -m "feat(theme): 首頁以 data-theme=\"light\" 隔離於深色模式外

首頁是影片背景＋塗鴉的遊樂場，沒有可反轉的主題表面，但它讀 guide token，
而 .dark 掛在 html 上是全站的——不隔離的話 GuideEntry 的便條紙會變成
白底配淺藍字。用子樹 token 重設隔離，diff 比改硬編值小 4 倍。

同時立下不變式：首頁那五個檔案不得使用 dark: utility（token 重設擋不住它）。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: 零星功能色的 `dark:` variant

不適合抽成 token 的獨立判斷色。

**Files:**

- Modify: `src/components/guide/RankChangeBadge.tsx:20`
- Modify: `src/components/pokopia/PokopiaPage.tsx`（`bg-black/40` scrim）
- Modify: 其餘 `grep` 命中的零星色

**Interfaces:**

- Consumes: Task 3 的 `@custom-variant dark`。

- [ ] **Step 1: 修 `RankChangeBadge` 的持平色**

`CHANGE_INK.same`（`#69737e`）是純文字色，直接踩在頁面背景上——深色模式下對比不足。
漲跌那兩個是**淺底藥丸**（自帶 `#fce7ec` / `#e2f3ec` 底），內部對比不受頁面背景影響，**不用動**。

把 `src/components/guide/RankChangeBadge.tsx` 的 `same` 分支改成用 CSS 變數，
讓深色模式能覆寫（inline style 沒辦法寫 `dark:`）：

在 `styles.css` 的 `:root` 加：

```css
/* RankChangeBadge 的「持平」色。漲跌兩色自帶淺底藥丸不受背景影響，
   * 只有這個是純文字色直接踩在頁面背景上，必須分模式給值。 */
--rank-same-ink: #69737e;
```

在 `.dark` 區塊加：

```css
--rank-same-ink: #93a1ad;
```

`RankChangeBadge.tsx` 第 20 行，把常數改成變數引用：

```tsx
const CHANGE_INK = {
  up: "#e30041", // 漲＝紅。白底 4.84、Tier S 底 4.63
  down: "#008251", // 跌＝綠。白底 4.87、Tier S 底 4.66
  same: "var(--rank-same-ink)", // 純文字色，深色模式要提亮，見 styles.css
} as const;
```

- [ ] **Step 2: 修 `CopyDecklist` 的失敗訊息色**

全站零星功能色**只有這一處**（其餘都被 Task 1／2 的 token 收斂吸收了）。
`src/components/guide/CopyDecklist.tsx:61` 的 `text-red-700`（`#b91c1c`）
在深色底 `#0f1a22` 上只有約 2.5:1。改成：

```tsx
        className={`text-sm ${state === "failed" ? "text-red-700 dark:text-red-400" : "text-guide-ink"}`}
```

`text-red-400`（`#f87171`）在 `#0f1a22` 上約 6.4:1，在 Step 4 實測確認。

- [ ] **Step 3: 確認 scrim 與其餘不動的部分**

```bash
grep -rn "bg-black/" src --include=*.tsx
grep -rnE "(text|bg|border)-(emerald|rose|red|amber|green)-[0-9]{2,3}" src --include=*.tsx
```

第一個預期 2 處：`PokopiaPage.tsx`（`bg-black/40`）與 `VideoInspiration.tsx`（`bg-black/55`）。
pro-rules 要求 scrim 落在 40–60%，**兩者都在範圍內，不改**，Task 10 目視確認即可。

第二個預期只剩 Step 2 剛改的那行，加上 `RankChangeBadge.tsx:9` 的**註解**
（那是歷史記錄，寫著原本用過哪些色，不是實際使用，保留不動）。

**以下刻意完全不動，不是漏了：**

| 對象                         | 為什麼不動                                                                                                                                                       |
| ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `TierBadge.tsx`（10 組色階） | 語意色晶片，淺底深字，對比是晶片內部的，不受頁面背景影響                                                                                                         |
| `EnergyIcon.tsx`（屬性色）   | 同上                                                                                                                                                             |
| `RankChangeBadge` 的漲跌兩色 | 自帶淺底藥丸（`#fce7ec` / `#e2f3ec`），同上                                                                                                                      |
| `src/lib/error-page.ts`      | 完全獨立的 HTML 字串（自帶 `<html>` 與 `<style>`），是 h3 層級錯誤的最後防線。讀不到 token，也沒有 `.dark` class——要支援深色得在字串裡再實作一次三態解析，不划算 |
| 首頁那五個檔案               | 由 Task 5 的 `data-theme="light"` 隔離                                                                                                                           |

- [ ] **Step 4: 兩個模式各量一次**

對 `/decks` 跑 `__contrast()`，深淺各一次。

**特別確認排行榜的持平列**：找一列 `—` 的徽章，深色模式下量它的對比。

```js
document.documentElement.classList.add("dark");
__contrast().fails.filter((f) => f.text.includes("—"));
```

預期：空陣列。

**`CopyDecklist` 的失敗態不容易自然觸發**（要 clipboard API 失敗），直接算色值：

```js
// 借用工具 A 的 ratio 邏輯，手動比對 text-red-400 對深色頁面底
__contrast; // 確認已定義
const probe = document.createElement("span");
probe.className = "text-red-400";
document.querySelector("main").appendChild(probe);
getComputedStyle(probe).color; // 應為 rgb(248, 113, 113)
probe.remove();
```

把該值與 `--guide-bg`（`#0f1a22`）代入對比公式，預期 ≥4.5:1。

- [ ] **Step 5: Commit**

```bash
git add src/styles.css src/components/guide/RankChangeBadge.tsx src/components/pokopia
git commit -m "feat(theme): 零星功能色補上深色對應值

RankChangeBadge 的「持平」色改走 CSS 變數（inline style 寫不了 dark:），
深色模式提亮到 #93a1ad。漲跌兩色不動——它們自帶淺底藥丸，內部對比
不受頁面背景影響。

scrim（bg-black/40 與 /55）維持原值，都在 pro-rules 的 40-60% 範圍內。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: 主題狀態（`src/lib/theme.ts`）

**Files:**

- Create: `src/lib/theme.ts`

**Interfaces:**

- Produces: 以下匯出，Task 8（inline script）與 Task 9（ThemeToggle）會用到。**函式名與型別名不要改**：

```ts
export type ThemePreference = "system" | "light" | "dark";
export type ResolvedTheme = "light" | "dark";
export const THEME_STORAGE_KEY: "piplup-theme";
export function readPreference(): ThemePreference;
export function resolveTheme(pref: ThemePreference): ResolvedTheme;
export function applyTheme(resolved: ResolvedTheme): void;
export function setPreference(pref: ThemePreference): void;
export function subscribeSystemChange(onChange: (resolved: ResolvedTheme) => void): () => void;
```

- [ ] **Step 1: 建立 `src/lib/theme.ts`**

```ts
/**
 * 深色模式的狀態層：三態偏好、localStorage、系統偏好監聽、套用到 <html>。
 *
 * ⚠ 這裡的解析邏輯在 `src/routes/__root.tsx` 的 <head> inline script 裡有
 * **第二份**。那一份必須是同步、無相依、自包含的（首次繪製前就要跑完，
 * 那時模組還沒載入），所以無法共用這個檔案。**兩份要一起改。**
 */

export type ThemePreference = "system" | "light" | "dark";
export type ResolvedTheme = "light" | "dark";

export const THEME_STORAGE_KEY = "piplup-theme";

/** 淺色沿用 __root.tsx 既有的 theme-color，不能改——那會動到淺色模式的行為。 */
const THEME_COLOR: Record<ResolvedTheme, string> = {
  light: "#2a6f97",
  dark: "#0f1a22",
};

const DARK_QUERY = "(prefers-color-scheme: dark)";

function isPreference(value: unknown): value is ThemePreference {
  return value === "system" || value === "light" || value === "dark";
}

/** 讀使用者存的偏好。localStorage 不可用（隱私模式）或值不合法時回 "system"。 */
export function readPreference(): ThemePreference {
  try {
    const stored = localStorage.getItem(THEME_STORAGE_KEY);
    return isPreference(stored) ? stored : "system";
  } catch {
    return "system";
  }
}

/** 把偏好解析成實際要套用的主題。SSR（沒有 window）一律回 "light"。 */
export function resolveTheme(pref: ThemePreference): ResolvedTheme {
  if (pref !== "system") return pref;
  if (typeof window === "undefined") return "light";
  return window.matchMedia(DARK_QUERY).matches ? "dark" : "light";
}

/**
 * 套用主題到 <html>。除了 class 之外還要設 colorScheme——否則深色頁面會配
 * 白色捲軸、表單控制項與 autofill 底色也不會跟著變。
 */
export function applyTheme(resolved: ResolvedTheme): void {
  const root = document.documentElement;
  root.classList.toggle("dark", resolved === "dark");
  root.style.colorScheme = resolved;
  const meta = document.querySelector('meta[name="theme-color"]');
  if (meta) meta.setAttribute("content", THEME_COLOR[resolved]);
}

/** 寫入偏好並立即套用。localStorage 寫入失敗時仍會套用（只是不會被記住）。 */
export function setPreference(pref: ThemePreference): void {
  try {
    localStorage.setItem(THEME_STORAGE_KEY, pref);
  } catch {
    // 隱私模式擋掉寫入：不阻斷切換，只是重新整理後回到系統偏好
  }
  applyTheme(resolveTheme(pref));
}

/**
 * 訂閱系統偏好變化。只有 pref === "system" 時需要——使用者在作業系統層切換
 * 時頁面要即時反應。回傳取消訂閱函式。
 */
export function subscribeSystemChange(onChange: (resolved: ResolvedTheme) => void): () => void {
  if (typeof window === "undefined") return () => {};
  const mql = window.matchMedia(DARK_QUERY);
  const handler = (e: MediaQueryListEvent) => onChange(e.matches ? "dark" : "light");
  mql.addEventListener("change", handler);
  return () => mql.removeEventListener("change", handler);
}
```

- [ ] **Step 2: 型別檢查**

```bash
npx tsc --noEmit
```

預期：0 錯誤。

- [ ] **Step 3: 在 console 手動驗證**

起 preview，導到 `/decks`，在 console：

```js
// 模組還沒被任何元件 import，所以用動態 import 驗
const t = await import("/src/lib/theme.ts");
t.setPreference("dark"); // 頁面應該立刻變深
t.readPreference(); // 應回 "dark"
t.setPreference("system"); // 應回到跟隨系統
localStorage.getItem("piplup-theme"); // 應為 "system"
```

預期：三次呼叫都如註解所述，且 `document.documentElement.style.colorScheme` 跟著變。

- [ ] **Step 4: Commit**

```bash
git add src/lib/theme.ts
git commit -m "feat(theme): 新增主題狀態層 theme.ts

三態偏好（system／light／dark）、localStorage 持久化、matchMedia 監聽、
套用 class 與 colorScheme 到 <html>。

colorScheme 不能省——否則深色頁面會配白色捲軸與淺色表單控制項。
localStorage 讀寫都包 try/catch，隱私模式下退回 system 而非中斷。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 8: 防閃 inline script

SSR 產出的 HTML 不知道使用者偏好。等 React hydrate 後才加 class 的話，深色模式使用者會先看到一幀白底。

**Files:**

- Modify: `src/routes/__root.tsx`

**Interfaces:**

- Consumes: Task 7 的 `THEME_STORAGE_KEY` 值（`"piplup-theme"`）與解析規則——**這裡是第二份實作，必須與 `theme.ts` 保持一致**。

- [ ] **Step 1: 在 `__root.tsx` 加 script 常數**

在 `import` 之後、`NotFoundComponent` 之前加：

```tsx
/**
 * 防止深色模式首畫面白閃。
 *
 * SSR 產出的 HTML 不知道使用者偏好，若等 hydrate 後才加 class，深色模式
 * 使用者會先看到一幀白底。這段必須是**同步**、無相依、自包含的——它要在
 * 首次繪製前跑完，那時 src/lib/theme.ts 還沒載入。
 *
 * ⚠ 這是 src/lib/theme.ts 解析邏輯的**第二份實作**。兩份要一起改，
 * 否則 inline script 與 React 端會對同一個偏好給出不同結果。
 */
const THEME_INIT_SCRIPT = `
(function () {
  try {
    var p = localStorage.getItem("piplup-theme");
    if (p !== "light" && p !== "dark") p = "system";
    var r = p === "system"
      ? (window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light")
      : p;
    var el = document.documentElement;
    if (r === "dark") el.classList.add("dark");
    el.style.colorScheme = r;
  } catch (e) {
    /* localStorage 被擋：什麼都不做，維持 SSR 的淺色 */
  }
})();
`;
```

- [ ] **Step 2: 把 script 塞進 `<head>`**

修改 `RootShell`：

```tsx
function RootShell({ children }: { children: ReactNode }) {
  return (
    <html lang="zh-Hant" suppressHydrationWarning>
      <head>
        <HeadContent />
        {/* 必須在 HeadContent 之後——theme-color meta 要先存在才改得到。
            同步執行，擋住首次繪製，這是刻意的：它只做幾個字串比較。 */}
        <script dangerouslySetInnerHTML={{ __html: THEME_INIT_SCRIPT }} />
      </head>
      <body>
        {children}
        <Scripts />
      </body>
    </html>
  );
}
```

`suppressHydrationWarning` 是必要的——script 會在 hydrate 前修改 `<html>` 的
`class` 與 `style`，React 會抱怨伺服器與用戶端不一致。

- [ ] **Step 3: 驗證無白閃**

```bash
npm run dev
```

（此步驟例外允許直接看，但**起 server 仍用 `preview_start`**。）

在瀏覽器先設成深色：console 跑 `localStorage.setItem("piplup-theme","dark")`，
然後**硬重新整理**（Ctrl+Shift+R）。

驗證方式——用 Performance 面板錄製重新整理，逐幀看首個繪製幀的背景色：

```js
// 或用這個較簡單的方式：在 head script 執行後立刻讀背景
performance.getEntriesByType("paint");
```

預期：首個 `first-contentful-paint` 時 `<html>` 已帶 `dark` class。
**目視確認沒有白色閃爍。**

- [ ] **Step 4: 驗證 localStorage 被擋時不會炸**

console：

```js
// 模擬隱私模式
const orig = Storage.prototype.getItem;
Storage.prototype.getItem = () => {
  throw new Error("blocked");
};
location.reload();
```

預期：頁面正常載入為淺色，console 無未捕捉錯誤。（測完重新整理即可還原。）

- [ ] **Step 5: 確認 console 無錯誤**

**注意**：`read_console_messages` 會回傳**保留緩衝**，重啟 dev server 後仍會吐出
同一個 Vite HMR 時間戳的舊錯誤（見 `docs/handoff.md`）。判斷當下是否真有錯誤，
要看時間戳有沒有變，或自己裝監聽器：

```js
window.__errs = [];
window.addEventListener("error", (e) => window.__errs.push(e.message));
// 操作一輪後
window.__errs;
```

預期：空陣列。

- [ ] **Step 6: Commit**

```bash
git add src/routes/__root.tsx
git commit -m "feat(theme): head inline script 防止深色模式首畫面白閃

SSR 的 HTML 不知道使用者偏好，等 hydrate 才加 class 會先閃一幀白底。
這段同步 script 在首次繪製前寫好 class 與 colorScheme。

<html> 加 suppressHydrationWarning——script 會在 hydrate 前改 class 與
style，否則 React 會回報伺服器／用戶端不一致。

⚠ 這是 theme.ts 解析邏輯的第二份實作，兩處都留了互相指向的註解。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 9: ThemeToggle 元件與 layout 置入

**Files:**

- Create: `src/components/ThemeToggle.tsx`
- Modify: `src/components/guide/GuideLayout.tsx`
- Modify: `src/components/pokopia/PokopiaLayout.tsx`

**Interfaces:**

- Consumes: Task 7 的 `readPreference`、`setPreference`、`resolveTheme`、`subscribeSystemChange`、型別 `ThemePreference`。
- Produces: `export function ThemeToggle(): JSX.Element`，無 props。

- [ ] **Step 1: 先量 `/pokopia` 手機 header 的實際預算**

**這一步不能跳過。** `PokopiaLayout` 剛從 109px 壓回 61px，375px 下只剩約 45px 餘裕，
塞一個 44px 按鈕會剛好吃光甚至折行。

用 `resize_window` 設成 375×812，導到 `/pokopia`，console：

```js
const nav = document.querySelector("header nav");
const used = [...nav.children].reduce((s, el) => s + el.getBoundingClientRect().width, 0);
const gaps = (nav.children.length - 1) * parseFloat(getComputedStyle(nav).columnGap);
({
  available: nav.clientWidth,
  used: used + gaps,
  spare: nav.clientWidth - used - gaps,
  headerHeight: document.querySelector("header").getBoundingClientRect().height,
});
```

記下 `spare`。**`headerHeight` 目前應為 61**。

- [ ] **Step 2: 建立 `ThemeToggle.tsx`**

```tsx
import { useEffect, useRef, useState } from "react";

import {
  readPreference,
  setPreference,
  subscribeSystemChange,
  applyTheme,
  type ThemePreference,
} from "@/lib/theme";

const OPTIONS: { value: ThemePreference; label: string }[] = [
  { value: "system", label: "跟隨系統" },
  { value: "light", label: "淺色" },
  { value: "dark", label: "深色" },
];

/**
 * 三態主題切換：44×44 按鈕開啟小選單。
 *
 * 不用 segmented control——/pokopia 手機 header 的寬度預算塞不下三個並排選項。
 * 也不用單鈕循環——使用者無法預期「下一態」是什麼。
 *
 * ⚠ 按鈕圖示用 **CSS**（dark:hidden / hidden dark:inline）切換，不是 React
 * state。SSR 與首次 client render 的輸出因此完全一致，避開 hydration mismatch。
 * 「目前選中哪一態」的 state 只在選單打開後才用到，那時已經 hydrate 完畢。
 */
export function ThemeToggle() {
  const [open, setOpen] = useState(false);
  const [pref, setPref] = useState<ThemePreference>("system");
  const rootRef = useRef<HTMLDivElement>(null);

  // 掛載後才讀 localStorage——SSR 讀不到，且首次 render 必須與伺服器一致
  useEffect(() => {
    setPref(readPreference());
  }, []);

  // 只有 system 態要監聽作業系統的切換
  useEffect(() => {
    if (pref !== "system") return;
    return subscribeSystemChange(applyTheme);
  }, [pref]);

  // 點外面關閉
  useEffect(() => {
    if (!open) return;
    const onDown = (e: MouseEvent) => {
      if (!rootRef.current?.contains(e.target as Node)) setOpen(false);
    };
    document.addEventListener("mousedown", onDown);
    return () => document.removeEventListener("mousedown", onDown);
  }, [open]);

  const choose = (value: ThemePreference) => {
    setPreference(value);
    setPref(value);
    setOpen(false);
  };

  return (
    <div ref={rootRef} className="relative">
      <button
        type="button"
        aria-label="切換主題"
        aria-expanded={open}
        aria-haspopup="menu"
        onClick={() => setOpen((v) => !v)}
        onKeyDown={(e) => e.key === "Escape" && setOpen(false)}
        className="inline-flex h-11 w-11 items-center justify-center rounded-full transition hover:bg-guide-bg-highlight"
      >
        {/* 圖示靠 CSS 切換，避開 hydration mismatch */}
        <svg
          className="h-5 w-5 dark:hidden"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth={2}
          strokeLinecap="round"
          aria-hidden="true"
        >
          <circle cx="12" cy="12" r="4" />
          <path d="M12 2v2M12 20v2M2 12h2M20 12h2M4.9 4.9l1.4 1.4M17.7 17.7l1.4 1.4M4.9 19.1l1.4-1.4M17.7 6.3l1.4-1.4" />
        </svg>
        <svg
          className="hidden h-5 w-5 dark:block"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth={2}
          strokeLinecap="round"
          strokeLinejoin="round"
          aria-hidden="true"
        >
          <path d="M21 12.8A9 9 0 1 1 11.2 3a7 7 0 0 0 9.8 9.8Z" />
        </svg>
      </button>

      {open && (
        <div
          role="menu"
          className="absolute right-0 z-50 mt-1 min-w-32 rounded-lg border border-guide-tint bg-guide-surface py-1 shadow-lg"
          onKeyDown={(e) => e.key === "Escape" && setOpen(false)}
        >
          {OPTIONS.map((opt) => (
            <button
              key={opt.value}
              type="button"
              role="menuitemradio"
              aria-checked={pref === opt.value}
              onClick={() => choose(opt.value)}
              className="flex min-h-11 w-full items-center gap-2 px-3 text-left text-sm whitespace-nowrap"
            >
              <span aria-hidden="true" className="w-4">
                {pref === opt.value ? "✓" : ""}
              </span>
              {opt.label}
            </button>
          ))}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 3: 放進 `GuideLayout`**

`src/components/guide/GuideLayout.tsx`：加 import，並把跨區膠囊的 `ml-auto` 移到
一個包住「膠囊 ＋ 切換鈕」的容器上，讓兩者一起靠右：

```tsx
import { ThemeToggle } from "@/components/ThemeToggle";
```

把原本的 `<Link to="/pokopia" className="ml-auto ...">` 改成：

```tsx
<div className="ml-auto flex items-center gap-1">
  <Link
    to="/pokopia"
    className="inline-flex min-h-11 items-center gap-1 rounded-full border border-guide-tint px-3.5 text-sm font-semibold whitespace-nowrap text-guide-ink transition hover:border-guide-accent hover:bg-guide-bg"
  >
    Pokopia 建築
    <span aria-hidden="true">→</span>
  </Link>
  <ThemeToggle />
</div>
```

（原本 `Link` 上的 `ml-auto` 要拿掉，其餘 class 逐字保留。）

- [ ] **Step 4: 放進 `PokopiaLayout`，並依 Step 1 的實測調整**

同樣把跨區膠囊與 `ThemeToggle` 包進 `ml-auto` 容器。

`ThemeToggle` 的 hover 底色在 pokopia 側要換成暖色。在 `ThemeToggle.tsx` 的按鈕
class 加 scope 感知：

```tsx
className =
  "inline-flex h-11 w-11 items-center justify-center rounded-full transition hover:bg-guide-bg-highlight [[data-scope=pokopia]_&]:hover:bg-pokopia-highlight";
```

**若 Step 1 量到的 `spare` < 44**：手機版把跨區膠囊的文字縮成圖示。

圖示用與 header logo 同族的 inline SVG（`strokeWidth={3}`、`strokeLinecap="round"`、
`strokeLinejoin="round"`），**不要用 emoji**——違反 pro-rules 的 `icon-style-consistent`，
而且 emoji 在不同平台的字面樣式不一致。

`PokopiaLayout` 的跨區 `Link` 改成：

```tsx
<Link
  to="/decks"
  aria-label="前往牌組攻略"
  className="inline-flex min-h-11 items-center gap-1 rounded-full border border-pokopia-tint px-3.5 text-sm font-semibold whitespace-nowrap text-pokopia-ink transition hover:border-pokopia-accent hover:bg-pokopia-highlight"
>
  {/* 手機只留圖示（寬度預算不足），sm 以上還原文字 */}
  <svg
    width="16"
    height="16"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    strokeWidth={3}
    strokeLinecap="round"
    strokeLinejoin="round"
    className="sm:hidden"
    aria-hidden="true"
  >
    <rect x="3" y="4" width="12" height="16" rx="2" />
    <path d="M17 7h2a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2h-2" />
  </svg>
  <span className="hidden sm:inline">牌組攻略</span>
  <span aria-hidden="true">→</span>
</Link>
```

`aria-label` 掛在 `Link` 上是關鍵——手機版沒有可見文字時，螢幕閱讀器靠它念出目的地。
桌機版有文字時 `aria-label` 仍會蓋過文字內容，兩者語意一致所以沒問題。

- [ ] **Step 5: 重量手機 header 高度**

375×812 下對 `/pokopia` 與 `/decks` 各跑一次 Step 1 的量測。

預期：**`headerHeight` 兩邊都 ≤61px**，`spare` ≥0，無水平溢位：

```js
document.documentElement.scrollWidth <= document.documentElement.clientWidth;
```

預期：`true`。

- [ ] **Step 6: 驗證觸控目標**

```js
[...document.querySelectorAll("header a, header button")]
  .map((el) => ({ t: el.textContent.trim().slice(0, 10), ...el.getBoundingClientRect().toJSON() }))
  .filter((r) => r.width < 44 || r.height < 44);
```

預期：空陣列。

- [ ] **Step 7: 驗證切換與鍵盤操作**

- 點按鈕 → 選單開啟，`aria-expanded="true"`
- 選「深色」→ 頁面立刻變深，選單關閉
- 重新整理 → 仍是深色
- 選「跟隨系統」→ 在作業系統切換深／淺，頁面即時反應
- Tab 到按鈕 → 焦點環可見（沒有被 `focus:outline-none` 關掉）
- 選單開啟時按 Escape → 關閉
- 點選單外面 → 關閉

- [ ] **Step 8: 確認無 hydration 警告**

硬重新整理，看 console。預期：**沒有** "Hydration failed" 或 "server rendered HTML didn't match" 之類的訊息。

- [ ] **Step 9: Commit**

```bash
git add src/components/ThemeToggle.tsx src/components/guide/GuideLayout.tsx src/components/pokopia/PokopiaLayout.tsx
git commit -m "feat(theme): 新增三態主題切換鈕並放進兩個 layout

44x44 按鈕開啟小選單（系統／淺色／深色）。不用 segmented control——
/pokopia 手機 header 的寬度預算塞不下；也不用單鈕循環——使用者無法
預期下一態。

按鈕圖示靠 CSS（dark:hidden）切換而非 React state，SSR 與首次 render
輸出一致，避開 hydration mismatch。

手機 header 高度維持 61px、觸控目標全數 44px、無水平溢位。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 10: 全站雙模式驗收與交接文件

**Files:**

- Modify: `docs/handoff.md`

**Interfaces:**

- Consumes: Task 1–9 的全部成果。

- [ ] **Step 1: 七頁 × 兩模式對比全掃**

對這七個目標各跑一次「工具 A」，深淺各一次（共 14 次）：

| 頁面                      | 量測前要先做的事                 |
| ------------------------- | -------------------------------- |
| `/decks`                  | 展開數列排行榜、切一次 Tier 篩選 |
| `/decks/mega-blaziken-ex` | 捲到底，含對戰思路與 pager       |
| `/decks/schedule`         | 無                               |
| `/pokopia`                | 展開篩選器、點開一個建築詳情     |
| `/pokopia/videos`         | 無                               |
| `/pokopia/habitats`       | 選一個棲息地                     |
| `/no-such-page`（404）    | 無                               |

預期：**14 次全部 `failCount: 0`**。

記錄每次的 `checked` 節點數，寫進 handoff。

- [ ] **Step 2: 非文字對比**

深色模式下，量兩條進度條的填色 vs 軌道（WCAG 1.4.11 要 ≥3:1）：

```js
const bar = document.querySelector(".bg-guide-ink"); // CardUsagePanel（在攻略頁）
const track = bar.parentElement;
[getComputedStyle(bar).backgroundColor, getComputedStyle(track).backgroundColor];
```

用「工具 A」裡的 `ratio` 函式算兩者的比值。對 `/pokopia` 的 `bg-pokopia-accent`
進度條重複一次。

預期：兩者都 ≥3:1。

同樣量 header 底部邊框對背景（`--guide-tint` / `--pokopia-tint` 對各自的 bg）。

- [ ] **Step 3: 首頁隔離最終確認**

深色模式下導到 `/`：

```js
getComputedStyle(document.querySelector("h1")).color; // 應為 rgb(42, 111, 151)
__contrast(); // failCount: 0
```

再跑一次不變式檢查：

```bash
grep -rn "dark:" src/routes/index.tsx src/components/GuideEntry.tsx src/components/PokopiaEntry.tsx src/components/Doodles.tsx src/components/VideoBackdrop.tsx
```

預期：無命中。

- [ ] **Step 4: 跨頁導覽一致性**

深色模式下走 `/decks` → `/pokopia` → `/` → `/decks`，每一步確認：
攻略頁與 Pokopia 頁維持深色、首頁維持淺色、切換鈕圖示正確（月亮）。

- [ ] **Step 5: 回歸檢查**

```bash
npx tsc --noEmit && npm run lint && npm run build
```

預期：三者都通過。build 會跑 `prebuild`（`subset-cards.mjs` ＋ `generate-sitemap.mjs`）。

確認 client 卡片索引 chunk 沒有回退：

```bash
ls -la .output/public/_build/assets/ | grep -i card
```

預期：約 19 KB，**不是** 560 KB 那個等級。

- [ ] **Step 6: 自裝監聽器驗 console**

工具緩衝不可信（見 `docs/handoff.md`）。在每個頁面：

```js
window.__errs = [];
addEventListener("error", (e) => __errs.push(e.message));
addEventListener("unhandledrejection", (e) => __errs.push(String(e.reason)));
// 互動一輪（切換主題、展開列、開選單）後
__errs;
```

預期：空陣列。

- [ ] **Step 7: 更新 `docs/handoff.md`**

在「2026-07-24」段落後新增一節，記錄：

- 深色模式的三層架構與檔案位置
- **兩份解析邏輯**（`theme.ts` 與 `__root.tsx` 的 inline script）必須同步修改
- **首頁隔離機制**與「那五個檔案不得用 `dark:`」的不變式
- token 對照表（淺／深）
- 兩個模式的實測對比數字（Step 1 的 14 次結果）
- 語意色晶片刻意不做深色版的理由

同時更新「目前狀態」表格的 Theming 列（目前標「待重新稽核」）。

- [ ] **Step 8: Commit**

```bash
git add docs/handoff.md
git commit -m "docs(handoff): 記錄深色模式的架構、不變式與實測數字

七頁 x 兩模式共 14 次對比量測全數 0 失敗（/decks 的硬底線維持）。
記下兩個日後最容易踩壞的點：theme.ts 與 inline script 的雙份解析邏輯、
首頁那五個檔案不得使用 dark: utility。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 收工檢查

- [ ] 14 次對比量測全數 `failCount: 0`
- [ ] 淺色模式算繪值與改動前逐字元相同（Task 1／2 的快照 diff）
- [ ] `npx tsc --noEmit`、`npm run lint`、`npm run build` 全部通過
- [ ] client 卡片索引 chunk 未回退（~19 KB）
- [ ] 手機 375px：兩個 header ≤61px、觸控目標全 ≥44px、無水平溢位
- [ ] 無 hydration 警告、無 console 錯誤（自裝監聽器確認）
- [ ] 硬重新整理無白閃；隱私模式不報錯
- [ ] 首頁在深色模式下維持淺色
- [ ] `docs/handoff.md` 已更新
