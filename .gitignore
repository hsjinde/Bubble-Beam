# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Environment variables / secrets
.env
.env.*
!.env.example
*.pem
*.key

# Agent local config / logs / scratch (Claude Code, Antigravity CLI, etc.)
.claude/settings.local.json
# git worktree 的實體目錄——內容是同一個 repo 的另一份 checkout，
# 不能當成一般檔案 commit 進來（會變成巢狀 repo 內容）
.claude/worktrees/
.agy/
.antigravity/
agy.log
*.agy.log

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local-only background video (never publish downloaded video files)
public/videos/*.mp4
public/videos/*.webm
public/videos/manifest.json

# Cloned repositories / test scripts
pi/
